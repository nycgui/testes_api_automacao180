---
  default: --publish-quiet -p allure -p bdd -p local -p chrome_headless
  bdd: -f pretty
  dots: -f progress
  html: -f html -o logs/report.html
  allure: --format AllureCucumber::CucumberFormatter --out=logs
  local: CONFIG=local.yml
  hmg: CONFIG=hmg.yml
  firefox: BROWSER=selenium
  firefox_headless: BROWSER=selenium_headless
  chrome: BROWSER=selenium_chrome
  chrome_headless: BROWSER=selenium_chrome_headless